# ROLLBACK 0160–0163 (Nexus Link F4.1 · Fundación colaborativa)

> Devuelve la DB al estado exacto de `a6c23f9` (migs hasta `0159`). Ejecutar SOLO con
> autorización de Dirección, como UN batch por sección, en orden inverso (0163→0160).
> **Irreversible por diseño (declarado):** los estados ya drenados de `connect_outbox`,
> las notificaciones/menciones ya emitidas y las filas de `audit_log` NO se revierten
> (append-only). El rollback detiene la emisión futura y restaura funciones/policies.

## 0) Deshabilitar el scheduling (si el deploy ya salió)
- Re-publish del deploy previo (quita la Netlify Scheduled Function) — o esperar: sin las
  RPCs el route responde 502 sin efectos (fail-closed).

## 1) Revertir 0163 (guardas de archivado)
Restaurar los cuerpos VIGENTES pre-0163 re-ejecutando sus fuentes (idempotentes):
```sql
-- (a) Re-ejecutar ÍNTEGRA 0151_connect_moderation_failclose.sql
--     → restaura add_member / remove_member / set_member_role / set_topic / pin / unpin
--       (cuerpos fail-closed P-1, SIN guarda de archivado). NO usar 0144 para estas 6.
-- (b) Re-ejecutar ÍNTEGRA 0150_connect_join_channel.sql → restaura connect_join_channel.
-- (c) Re-ejecutar de 0144_connect_rpc.sql SOLO las secciones de:
--     connect_edit_message · connect_react · connect_unreact · connect_flag_message ·
--     connect_unflag_message · connect_link_entity · connect_unlink_entity
--     (create or replace + revoke/grant de cada una).
```

## 2) Revertir 0162 (acciones de notificación)
```sql
drop function if exists public.connect_notif_snooze(uuid, timestamptz);
drop function if exists public.connect_notif_delegate(uuid, uuid);
drop function if exists public.connect_notif_set_priority(uuid, text);
drop function if exists public._notif_assert_owner_or_delegate(uuid);

-- Policies originales (texto 0004:69-84):
drop policy if exists "notifications read own or role" on public.notifications;
create policy "notifications read own or role"
  on public.notifications for select
  using (
    user_id = auth.uid()
    or (role_target is not null and role_target = public.current_role())
    or public.current_role() = 'admin'
  );
drop policy if exists "notifications mark read own" on public.notifications;
create policy "notifications mark read own"
  on public.notifications for update
  using (user_id = auth.uid() or public.current_role() = 'admin')
  with check (user_id = auth.uid() or public.current_role() = 'admin');

-- Grant de UPDATE original (table-level, default de Supabase pre-0162):
grant update on public.notifications to authenticated;
```

## 3) Revertir 0161 (menciones + fan-out) — **UN SOLO BATCH (post_message atómico)**
```sql
drop trigger if exists trg_connect_mentions_notify on public.connect_message_mentions;
drop function if exists public._connect_notify_mention();

-- post_message: volver a la firma 5-args (cuerpo EXACTO de 0144:130-187).
drop function if exists public.connect_post_message(uuid, text, uuid, text, uuid[], uuid[]);
-- → Re-ejecutar de 0144_connect_rpc.sql la sección "connect_post_message" completa
--   (create or replace de la 5-args + revoke/grant 0144:186-187).

-- _connect_enqueue_message: volver al cuerpo de 0144 (sin rama DM).
-- → Re-ejecutar de 0144_connect_rpc.sql la sección "_connect_enqueue_message"
--   (0144:28-57, incluye revoke + drop/create del trigger trg_connect_messages_enqueue).

drop function if exists public._connect_assert_not_archived(uuid);
```
⚠️ Ejecutar el paso 3 DESPUÉS del paso 1: las funciones restauradas de 0163→(0151/0150/0144)
ya no referencian `_connect_assert_not_archived` y el drop final no rompe dependencias.

## 4) Revertir 0160 (worker)
```sql
drop function if exists public.connect_claim_batch(int, interval);
drop function if exists public.connect_mark_processed(bigint);
drop function if exists public.connect_mark_failed(bigint, text, int);
drop function if exists public.connect_recover_stuck();
drop function if exists public.connect_prune_outbox(interval);
drop function if exists public.connect_record_worker_run(timestamptz, int, boolean, int, int, int, int, int, int, int, int, numeric, numeric, text);
drop table if exists public.connect_worker_runs;
```
`connect_outbox` NO se toca (tabla y datos preexistentes de 0143).

## 5) Cierre
```sql
notify pgrst, 'reload schema';
```
Verificación post-rollback: `connect_post_message` = 1 sola función 5-args; policies de
notifications = texto 0004; `connect_worker_runs` inexistente; smoke de posteo/lectura OK.
