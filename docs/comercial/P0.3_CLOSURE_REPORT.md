# P0.3 — Cierre · Ficha 360° expone avance de etapa desde `calificado`

**Proyecto:** TOPS Nexus — Logística TOPS (Verotin S.A.)
**Fecha:** 2026-06-06
**Frente:** P0.3 (único) — exponer por UI la transición `calificado → propuesta`
**Entorno re-test:** app local (3030) → **Supabase PROD** (`arsksytgdnzukbmfgkju`), sesión Chrome admin
**Autor:** CTO de Release

---

## 1. Resultado ejecutivo

> ## 🟢 P0.3 CERRADO — botón nuevo validado + Write E2E completo V6–V9 en PROD
> El hueco de UI (`calificado` no exponía avance) quedó corregido con un cambio mínimo (1 archivo, ~18 líneas) que **reutiliza el backend ya existente** (`crm_advance_stage`, 0047). Re-ejecutado el Write E2E de punta a punta en PROD: **V6–V9 todos PASS**, onboarding automático (P0.2) confirmado, estado `ocupado` confirmado, Dashboard restaurado a baseline. Sin workarounds, sin reglas de negocio nuevas, sin tocar otros módulos/infra/Supabase/migraciones.

---

## 2. Cambio implementado

**Archivo único:** `src/app/(app)/comercial/oportunidades/[id]/Opportunity360View.tsx`
- Helper `forwardAdvance(estado)` → `{ to: "propuesta", label: "Pasar a propuesta" }` solo para `calificado`; `null` para el resto.
- Botón secundario en el header (color de `propuesta`, #ea580c) que dispara `run(() => advanceStage(o.id, "propuesta"))` — mismo camino ya cableado de los CTA `propuesta`/`negociacion` (gating `writable`, `isPending`, feedback, `router.refresh()` + `revalidatePath`).

**No tocado:** RPC/RLS/migraciones, `stage-actions.ts`, `crm-types.ts`, otros módulos, infra, Supabase. Las CTAs `propuesta→negociación` y `negociación→ganado` (ya existentes) quedaron intactas. `visita` fuera de scope (la UI no ofrece entrada a `visita`; incluirla sería un frente nuevo).

**Decisión:** botón **siempre habilitado** en `calificado` (fiel al backend: D-2 solo bloquea en `ganado`). Sin regla de negocio nueva.

---

## 3. Validación

### 3.1 Estática + staging (sin claves de app)
| Check | Resultado |
|---|---|
| `tsc --noEmit` | ✅ exit 0 |
| Harness staging `w4-loop-staging.mts` (pg · BEGIN…ROLLBACK · guard de URL) | ✅ **12/12 PASS · GO**, ROLLBACK limpio |
| Cadena re-confirmada | `calificado→propuesta→negociacion→ganado→ocupado` aceptada por la DB de staging; banding anti-doble-conteo correcto (proyectada −200 al reservar; comercial −200 al ganar; ambas vuelven al baseline al ocupar) |

La línea `crm_advance_stage(opp,'propuesta')` del harness es la **misma RPC** que invoca el botón nuevo (mapeo 1:1).

### 3.2 Write E2E en PROD (navegador, datos E2E identificados)
Lead creado vía **webhook real**: `POST /api/clientify/webhook/<token>` → HTTP 200 `{action: inserted}`. Datos: `clientify_id=e2e-p03-001`, LEAD-2026-0002 "E2E P03 Andromaco", tags `e2e/p03`.

| V | Hito | Resultado | Evidencia |
|---|---|---|---|
| (setup) | Lead → Calificar → Promover (ANMAT 200 m²) | ✅ | OPP-2026-0002 en `calificado`; contacto heredado |
| **P0.3** | **Botón nuevo `Pasar a propuesta` presente en `calificado`** | ✅ | Header con 2 acciones: "Validar capacidad y reservar" (azul) + **"Pasar a propuesta"** (naranja) |
| **V6a** | **Click botón nuevo → `propuesta`** | ✅ PASS | Header `PROPUESTA`; banner "Etapa actualizada"; stepper avanza |
| (reserva) | Reservar capacidad (Pedro Luján · Cubiculo ANMAT PA4) | ✅ | Header `PROPUESTA · RESERVADO`; "Capacidad reservada" |
| **V6b** | `propuesta→negociación→ganado` (CTAs existentes) | ✅ PASS | Header **GANADO · COMPROMETIDO** |
| **V6c** | **Onboarding automático (P0.2)** | ✅ PASS | Tab Onboarding `(1)`; **ONB-2026-0001 PENDIENTE 0%** + 5 tareas (RNE/croquis/plancheta/accesos/documentación) |
| **V7** | Completar onboarding | ✅ PASS | ONB-2026-0001 **COMPLETADO · 100%** |
| **V8** | `committed_state = ocupado` (anti-doble-conteo) | ✅ PASS | Header **GANADO · OCUPADO**; "capacidad ocupada" |
| **V9** | Dashboard refleja el ciclo y **restaura** | ✅ PASS | Física = Comercial = Proyectada = **37.5% / 3.770 m²**; "Sin compromisos CRM cargados" (el `ocupado` sale del snapshot) |

**Patrón confirmado en PROD:** reservar baja proyectada; ganar baja comercial; ocupar restaura ambas (el m² pasa a ocupación física del Twin, sin doble conteo).

---

## 4. Cleanup (datos E2E en PROD)

Entidades a borrar (SQL Editor, operado por el usuario — borrado definitivo en PROD por el mecanismo oficial). FKs: opp cascada a onboarding/tasks/stage_history/quotes/proposals; `lead.opportunity_id` = SET NULL; sin contrato (RESTRICT no aplica).

```sql
delete from public.crm_opportunities where id = '4edc2217-199a-4ce9-bb4a-358282d921e7'; -- OPP-2026-0002 (cascada ONB-2026-0001 + 5 tasks + stage_history)
delete from public.crm_leads          where id = '6bb83013-5f59-44f7-b7da-d399a91922f3'; -- LEAD-2026-0002
delete from public.clientify_sync_log  where clientify_id = 'e2e-p03-001';

-- Verificación (0 / 0 / 0):
select
  (select count(*) from public.crm_opportunities where id = '4edc2217-199a-4ce9-bb4a-358282d921e7') as opp,
  (select count(*) from public.crm_leads          where id = '6bb83013-5f59-44f7-b7da-d399a91922f3') as lead,
  (select count(*) from public.clientify_sync_log where clientify_id = 'e2e-p03-001')               as synclog;
```

> Nota: la opp quedó en `ocupado` → **ya no está en el snapshot del Dashboard** (no lo skewea). El cleanup es por higiene (RR-1), no time-sensitive como el del E2E parcial.

### Entorno
- ✅ `.env.local` restaurado (secret temporal removido).
- ✅ Dev server reiniciado → webhook **fail-closed** verificado (token previo → 401).

---

## 5. Veredicto

- **P0.3:** 🟢 **CERRADO** — botón nuevo implementado, validado en staging (12/12) y en PROD (V6a).
- **Write E2E completo (V1–V9):** 🟢 **GO** — el flujo Lead→…→Ocupado→Dashboard opera de punta a punta por UI, exactamente como en producción.
- **Hallazgo original:** ✅ resuelto. El pipeline comercial es operable íntegramente desde la interfaz.

### Pendiente (no en este frente)
- Cleanup PROD (§4) — ejecutar y confirmar 0/0/0.
- Merge/PR/deploy a Netlify — frente aparte, sin autorización en esta entrega.
